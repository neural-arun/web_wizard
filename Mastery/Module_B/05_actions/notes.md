# Actions
 - python do things what humans does.

page.goto("https://google.com")
page.fill("input[name='q']", "AI")
page.press("input[name='q']", "Enter")

## working:
* finds elements in the DOM
* ensure the element is visible.
* ensure the element is enabled.
* scrolls if needed.
* performs the action.
# this does auto waiting before action automatically.

